# .codex Runtime Directory

Project-scoped Codex config and custom agents live here. Custom agents are in
`.codex/agents/*.toml`; `.codex/config.toml` sets conservative runtime
defaults.

Current root legion profile:

- `commander`: top-level router
- `no1-*`: 6 Founder Stage-Gate specialist agents
