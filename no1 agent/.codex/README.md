# .codex Runtime Directory

Project-scoped Codex config and custom agents live here. Custom agents are in `.codex/agents/*.toml`; `.codex/config.toml` sets conservative runtime defaults.

Default full lifecycle profile: 6 custom agents.
